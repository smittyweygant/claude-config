# codex/

Placeholder for a portable Codex CLI config (`AGENTS.md`, `config.toml`
template, an `install-codex.sh`), mirroring this repo's Claude Code setup —
not yet built. Tracked as a follow-up rather than guessed at, since it needs
its own pass to get the content right rather than copying Tyler's
Akka-specific `AGENTS.md`/MCP config wholesale.
