# templates/

Per-project scaffolding meant to be copied into a project root (not installed
globally) — e.g. a `.claudeignore` template or a project-level
`.claude/settings.json` template. Empty for now.
